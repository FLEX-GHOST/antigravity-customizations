{
  "name": "pin_skill_for_session",
  "description": "Pin a mission-critical skill or rule to session memory so it is prioritized in every subsequent agent action.",
  "inputSchema": {
    "properties": {
      "skill_id": {
        "title": "Skill Id",
        "type": "string"
      }
    },
    "required": [
      "skill_id"
    ],
    "title": "pin_skill_for_sessionArguments",
    "type": "object"
  },
  "metadata": {
    "readOnly": false,
    "isDestructive": false,
    "idempotent": false,
    "ttlMs": 0,
    "cacheScope": "private"
  }
}